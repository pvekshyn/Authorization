variables:
  needToUpdateAppImage: true
  needToUpdateDatabase: true
  needToRunUnitTests: true
  needToRunIntegrationTests: true
  needToRunAcceptanceTests: true

trigger:
  branches:
    include:
    - main
  paths:
    include:
    - Authorization

pool: 
  vmImage: ubuntu-latest

stages:
  - template: ../pipeline/deploy-api-service.yml
    parameters:
      serviceName: 'Authorization'