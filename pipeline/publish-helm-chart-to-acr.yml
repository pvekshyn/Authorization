steps:
  - task: HelmDeploy@0
    displayName: Publish Helm chart to ACR
    inputs:
      connectionType: Kubernetes Service Connection
      kubernetesServiceConnection: pvauthorizationaks-sc
      command: package
      chartPath: pipeline/helm/api-service
      destination: pvauthorizationcr.azurecr.io/helm/api-service
